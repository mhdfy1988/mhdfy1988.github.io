# 项目协作规则

## 项目形态

1. 当前项目是静态 GitHub Pages 主页，默认入口是 `index.html`。
2. 当前主页不是单页面应用；默认按“首页 + 多个独立工作区页面”的多页面结构维护。
3. 首页只做总控台、入口、状态和最近动态；卡片应跳转到独立目录页面，例如 `agent/`、`notes/`、`toolbox/`。
4. `agent/` 这类专题工作区页面只负责路线图、阶段进度、代码主线、阶段记录和下一步动作，不在正文里平铺知识库条目。
5. `notes/` 是独立知识库索引页，负责按条目和专题做检索入口；知识正文优先落到 `notes/` 自己的子目录，例如 `notes/agent/`，不要再挂回 `agent/` 目录。
6. `notes/index.html` 默认只做知识库分类入口页；优先使用分类卡片组织专题，不在该页混放工作区推进内容、长日志流或大段知识正文。
7. 没有构建流程时，优先保持 `index.html`、`styles.css`、`script.js` 这类轻量静态结构。
8. 本地验证可以临时启动静态 HTTP 服务，但必须说明临时端口；验证结束后要停止服务，不把临时端口当成最终访问入口。

## 图片与前端验证

1. 首页使用外链图片时，不能只依赖 `HEAD` 请求或状态码判断可用；必须用浏览器预览确认图片真实渲染。
2. 如果外链图片在浏览器中被拦截、显示空白或触发控制台错误，优先更换为浏览器可正常加载的图片，或改成本地静态资源。
3. 修改首页交互后，至少验证桌面端、移动端和关键点击路径；本项目当前关键路径是工作空间卡片跳转到独立工作区页面。
